import { Button } from "@chakra-ui/react";
import SideBar from "../../components/SideBar";
const SchoolListPage = () => {
  return <SideBar children={<Button>asdf</Button>} />;
};
export default SchoolListPage;
